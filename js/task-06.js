const textInput = document.querySelector('#validation-input');
const checkInput = textInput.value.length;

textInput.addEventListener('input', (event) => {
    const cur = event.currentTarget.value;
}
    

textInput.addEventListener('blur', () => {
    
})